let temp = 65

// AND - && : It's true if both expressions are true, otherwise are false.
if (temp >= 60 && temp <= 90){
    console.log('It is really nice out side!')
}

// OR - || : It's true if at least one expressions is true, otherwise is false.
else if (temp <= 0 || temp >= 120){
    console.log("Do note gou outside")
} else {
    console.log('Do what you want')
}